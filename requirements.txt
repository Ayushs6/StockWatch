# ── Web framework ─────────────────────────────────────────────────────────────
Flask==3.1.3
flask-cors==6.0.2

# ── HTTP / data ───────────────────────────────────────────────────────────────
requests==2.32.5
pandas==3.0.1
numpy==2.4.3
lxml==6.1.0                 # parse the S&P 500 table from Wikipedia
beautifulsoup4==4.14.3

# ── Market data providers ─────────────────────────────────────────────────────
massive==2.4.0             # Polygon-style REST client (search, snapshot, charts)
yfinance==1.3.0            # live price + Yahoo Finance news

# ── AI / ML ───────────────────────────────────────────────────────────────────
transformers==5.7.0        # FinBERT (ProsusAI/finbert) sentiment pipeline
torch==2.11.0              # backing tensor engine for FinBERT
sentence-transformers==5.4.1   # all-MiniLM-L6-v2 embeddings for vector search
groq==1.2.0                # primary LLM summariser (Llama 3.1 8B Instant)
google-genai==1.74.0       # Gemini fallback summariser

# ── Config ────────────────────────────────────────────────────────────────────
python-dotenv==1.0.1       # load API keys from a local .env file
